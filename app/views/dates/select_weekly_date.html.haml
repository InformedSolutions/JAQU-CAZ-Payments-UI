- page_title('Choose your dates')
= link_to 'Back', weekly_charge_dates_path, class: 'govuk-back-link', id: 'back-link'

%main.govuk-main-wrapper#main-content{role: 'main'}
  .govuk-grid-row
    .govuk-grid-column-two-thirds
      - if alert
        = render 'common/error_summary',
          error_msg_div: '#select-weekly-date-error',
          error_msg: alert

      %h1.govuk-heading-l Choose your dates
      %p
        A weekly charge allows you to drive in Leeds for 7 consecutive days.
      - if @all_paid
        .govuk-inset-text
          If the weekly charge you want to pay for is not available, 
          it is because you have paid for a date in those 7 consecutive days.
      %details.govuk-details{'data-module': 'govuk-details'}
        %summary.govuk-details__summary
          %span.govuk-details__summary-text
            Why can't I see my dates?
        .govuk-details__text
          %p
            If the date you want to pay for is not displayed, it may be because:
            %ul
              %li
                The period you had to pay has ended. A weekly charge must begin in the last 6 days.
              %li
                The date is too far in the future. You should check back here closer to the date you want.

      = form_tag confirm_date_weekly_dates_path, method: :post do
        .govuk-form-group{class: ('govuk-form-group--error' if alert)}
          %fieldset.govuk-fieldset
            %legend.govuk-visually-hidden
              Select weekly date

            - if alert
              %span#select-weekly-date-error.govuk-error-message
                %span.govuk-visually-hidden Error:
                = alert
            .govuk-radios
              - @dates.each_with_index do |date, index|
                .govuk-radios__item
                  %input.govuk-radios__input{'aria-describedby': 'date-hint',
                                             name: 'dates[]',
                                             type: 'radio',
                                             value: date[:value],
                                             id: "date-#{index}",
                                             checked: checked_weekly?(date[:value]),
                                             disabled: date[:disabled]}
                  %label.govuk-label.govuk-radios__label{for: "date-#{index}"}
                    = display_date(date)
                  %span#date-hint.govuk-hint.govuk-radios__hint
                    = date[:hint]
        = submit_tag 'Continue', class: 'govuk-button', 'data-module': 'govuk-button' if ! @all_paid

      %p= link_to 'Pay for 1 day instead', select_period_dates_path, id: 'pay_instead'
